export interface ProjectData {
  name: string,
  description: string,
  longDescription: string,
  imgPath: URL,
}

export interface TimePeriodData {
  title: string,
  subtitle: string,
  dateRange: string,
}